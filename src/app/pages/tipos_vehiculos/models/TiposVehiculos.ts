export interface TiposVehiculos {
    tiposVehi_id?: number,
    tiposVehi_nombre: string,
    tiposVehi_capacidad: string,
    tiposVehi_medida: string,
    updatedAt?: string | null,
    createdAt?: string | null,
}